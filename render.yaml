# render.yaml — Swalook Landing Page
services:
  - type: web
    name: swalook-landing-page
    env: static
    buildCommand: bun install && bun run build
    staticPublishPath: out
    envVars:
      - key: NEXT_PUBLIC_API_URL
        value: https://swalook-node.onrender.com
